# 🚀 Инструкция по деплою на Vercel

## ✅ Что уже сделано

1. ✅ Создан `postcss.config.js` - конфигурация PostCSS
2. ✅ Создан `tailwind.config.js` - конфигурация Tailwind CSS
3. ✅ Создан `vercel.json` - конфигурация Vercel
4. ✅ Обновлен `package.json` - добавлены зависимости для Tailwind
5. ✅ Удален CDN Tailwind из `index.html`
6. ✅ Локальная сборка работает успешно

## 📋 Шаги для деплоя

### 1. Настройка переменных окружения в Vercel

**ВАЖНО!** Без этого шага приложение не будет работать.

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте переменную:

   ```
   Name: GEMINI_API_KEY
   Value: ваш_ключ_API_от_Google_Gemini
   ```

5. Выберите все окружения: **Production**, **Preview**, **Development**
6. Нажмите **Save**

### 2. Деплой через Git (рекомендуется)

```bash
# Добавьте все изменения
git add .

# Создайте коммит
git commit -m "Configure for Vercel deployment with Tailwind CSS"

# Отправьте на GitHub
git push
```

Vercel автоматически обнаружит изменения и начнет деплой.

### 3. Альтернатива: Деплой через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Запустите деплой
vercel

# Для production деплоя
vercel --prod
```

## 🔍 Проверка после деплоя

1. Откройте URL вашего приложения на Vercel
2. Откройте консоль браузера (F12)
3. Проверьте, что нет ошибок
4. Убедитесь, что виджет отображается корректно

## ⚠️ Возможные проблемы и решения

### Ошибка: "API key must be set when running in a browser"

**Причина:** Не настроена переменная окружения `GEMINI_API_KEY`  
**Решение:** Добавьте `GEMINI_API_KEY` в Environment Variables в настройках Vercel

### Ошибка: "PostCSS Plugin was passed as a function"

**Причина:** Неправильная конфигурация PostCSS  
**Решение:** Уже исправлено в `postcss.config.js`

### Белый экран после деплоя

**Причина:** Ошибка в JavaScript или отсутствие переменных окружения  
**Решение:**

1. Проверьте консоль браузера
2. Проверьте логи деплоя в Vercel
3. Убедитесь, что все переменные окружения настроены

## 🔑 Где получить Gemini API Key

1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите с Google аккаунтом
3. Нажмите **Create API Key**
4. Скопируйте ключ

## 📊 Проверка статуса деплоя

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Во вкладке **Deployments** увидите статус последнего деплоя
4. Нажмите на деплой, чтобы увидеть подробные логи

## ✨ Готово

После успешного деплоя ваше приложение будет доступно по URL от Vercel.
