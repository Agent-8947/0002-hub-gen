<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# Multi-Contact Feedback Hub

Виджет обратной связи с поддержкой нескольких каналов коммуникации и AI-генерацией ответов.

## 🚀 Быстрый старт

### Локальная разработка

1. **Установите зависимости:**

```bash
npm install
```

1. **Создайте файл `.env.local`:**

```bash
GEMINI_API_KEY=your_gemini_api_key_here
```

1. **Запустите dev-сервер:**

```bash
npm run dev
```

1. **Откройте в браузере:**

```
http://localhost:3000
```

## 📦 Деплой на Vercel

### Шаг 1: Подготовка проекта

Убедитесь, что все файлы конфигурации на месте:

- ✅ `postcss.config.js`
- ✅ `tailwind.config.js`
- ✅ `vercel.json`
- ✅ `package.json` с зависимостями `tailwindcss`, `postcss`, `autoprefixer`

### Шаг 2: Настройка переменных окружения в Vercel

1. Откройте ваш проект в [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте переменную:
   - **Name:** `GEMINI_API_KEY`
   - **Value:** ваш ключ API от Google Gemini
   - **Environment:** Production, Preview, Development (выберите все)
4. Нажмите **Save**

### Шаг 3: Деплой

**Вариант A: Через Git (рекомендуется)**

```bash
git add .
git commit -m "Configure for Vercel deployment"
git push
```

Vercel автоматически задеплоит изменения.

**Вариант B: Через Vercel CLI**

```bash
npm install -g vercel
vercel
```

### Шаг 4: Проверка

После успешного деплоя:

1. Откройте URL вашего приложения
2. Проверьте, что виджет загружается без ошибок
3. Проверьте консоль браузера на наличие ошибок

## 🔑 Получение API ключа Gemini

1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите с вашим Google аккаунтом
3. Нажмите **Create API Key**
4. Скопируйте ключ и добавьте в переменные окружения

## 🛠️ Технологии

- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **Google Gemini AI** - генерация ответов

## 📝 Структура проекта

```
000-hub/
├── components/          # React компоненты
│   ├── AdminPanel.tsx  # Панель администратора
│   ├── Widget.tsx      # Виджет обратной связи
│   └── ...
├── services/           # Сервисы
│   └── telegramService.ts
├── config.ts           # Конфигурация
├── index.tsx           # Точка входа
├── App.tsx             # Главный компонент
└── index.css           # Глобальные стили
```

## 🐛 Решение проблем

### Ошибка: "PostCSS Plugin was passed as a function"

**Решение:** Убедитесь, что `postcss.config.js` экспортирует объект конфигурации, а не функцию.

### Ошибка: "API key must be set when running in a browser"

**Решение:** Добавьте `GEMINI_API_KEY` в переменные окружения Vercel.

### Ошибка: "@tailwind base is used but no matching directive"

**Решение:** Удалите CDN версию Tailwind из `index.html` и используйте локальную конфигурацию.

### Белый экран после деплоя

**Решение:**

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что все переменные окружения настроены
3. Проверьте, что сборка прошла успешно в логах Vercel

## 📄 Лицензия

MIT
